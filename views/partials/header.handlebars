<nav class="main-nav" role="navigation">
    <div class="top-menu">
        <div class="container">
            <div class="row d-flex align-items-center justify-content-between">
                <div id="logo"><a href="/">Home</a></div> 
                <div>
                    {{#if isLoggedIn}}
                        <a href="/stats" class="btn btn-primary mr-2">Stats</a>
                        <a href="/logout" class="btn btn-primary mr-2">Logout</a>
                    {{else}}
                        <a href="/login" class="btn btn-primary mr-2">Login</a>
                        <a href="/sign-up" class="btn btn-primary">Sign up</a>
                    {{/if}}
                </div>
            </div>
        </div>
    </div>
</nav>