import React, {Fragment} from 'react';
import { Header, Loader } from './helpers';

export default function() {
    return (
        <Fragment>
            <Header />
            <Loader />
        </Fragment>
    )
}